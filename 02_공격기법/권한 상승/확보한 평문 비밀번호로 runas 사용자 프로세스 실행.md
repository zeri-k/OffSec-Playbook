---
tags:
  - 환경/windows
시작조건: ["Windows 셸 또는 GUI 세션 확보", "다른 로컬·도메인 계정의 사용자명과 평문 비밀번호 확보"]
필요권한: ["현재 세션에서 runas 실행", "대상 계정의 로컬 로그온 권한"]
필요조건: ["<DOMAIN_OR_HOST>\\<USER>", "평문 비밀번호", "실행할 Windows 프로그램"]
결과: ["입력한 계정으로 실행되는 새 Windows 프로세스", "새 프로세스의 사용자·그룹·privilege·무결성 수준"]
---

# 확보한 평문 비밀번호로 runas 사용자 프로세스 실행

## 한 줄 판단

Windows 셸에서 다른 로컬·도메인 계정의 사용자명과 평문 비밀번호를 확보했으면 `runas /user`로 그 계정의 새 프로세스를 만들고 새 창의 `whoami /all`로 실제 사용자와 token 권한을 확인한다.

## 사용할 때

- 현재 가진 정보: 현재 계정과 다른 Windows 로컬·도메인 계정의 정확한 사용자명과 평문 비밀번호를 보유한다.
- 명령 실행 위치와 도달성: 대상 계정으로 프로세스를 만들 Windows 호스트에서 `runas.exe`와 대화형 비밀번호 prompt를 사용할 수 있다.
- 현재 계정과 권한: 현재 세션에서 프로세스를 실행할 수 있고, 대상 계정에는 해당 호스트의 로컬 로그온 권한이 있어야 한다.
- 성공 결과: 대상 계정의 로컬 token으로 실행되는 새 프로세스를 얻는다. Administrators 그룹 멤버십과 상승된 token은 별도로 확인한다.

## 전제 조건

| 확인할 것 | 필요한 상태 | 확인 방법 | 미충족 시 다음 확인 |
|---|---|---|---|
| 명령 실행 위치 | 프로세스를 만들 대상 Windows 호스트의 셸 또는 GUI 세션 | `hostname`, `whoami` | 원격 실행 호스트와 현재 prompt를 구분 |
| 현재 계정 또는 인증 수단 | `<DOMAIN_OR_HOST>\<USER>`와 평문 비밀번호 | 계정의 로컬·도메인 범위 확인 | NT hash·ticket만 있으면 이 기법을 사용하지 않음 |
| 현재 권한 | 현재 세션의 프로세스 생성과 대상 계정의 로컬 로그온 권한 | `runas` 결과와 새 프로세스 생성 확인 | 로그온 정책·계정 상태·프로그램 경로 확인 |
| 공격 대상의 조건 | 대상 계정이 현재 호스트에서 로컬 프로세스를 실행할 수 있음 | 새 프로세스의 `whoami /all` | 원격 서비스에만 유효한 계정이면 `/netonly` 기법 검토 |
| 필요한 파일·목록·주소 | 실행할 프로그램 경로 | `where cmd`, `where powershell` | 절대 경로와 인용부호 확인 |

## 실행

### 1. 현재 호스트와 사용자 확인

```cmd
hostname
whoami
```

### 2. 확보한 평문 비밀번호로 새 CMD 실행

```cmd
runas /user:<DOMAIN_OR_HOST>\<USER> "cmd.exe /k whoami /all"
```

표시되는 password prompt에 확보한 `<PASSWORD>`를 입력한다. 명령줄 인수에 비밀번호를 직접 넣지 않는다.

새 창에서 다시 확인한다.

```cmd
hostname
whoami
whoami /groups
whoami /priv
```

확인할 출력:

- `Attempting to start` 뒤 새 CMD 창이 열린다.
- 새 창의 `whoami`가 `<DOMAIN_OR_HOST>\<USER>`와 일치한다.
- 그룹·privilege·무결성 수준으로 일반 사용자, 관리자 그룹 멤버와 실제 상승 token을 구분한다.

## 관찰과 상태 전환

| 관찰 | 판단 | 결과 상태 | 다음 행동 |
|---|---|---|---|
| 새 프로세스의 `whoami`가 입력한 계정과 일치 | 평문 비밀번호와 로컬 로그온이 유효함 | 다른 Windows 계정의 프로세스 | [[Windows 셸 또는 세션 확보 후 컨텍스트 열거]]에서 계정·호스트·권한 재평가 |
| 새 프로세스에 상승된 관리자 token이 있음 | 현재 호스트의 고권한 프로세스 확보 | Windows 고권한 세션 | [[고권한 세션 확보 후 후속 판단]] |
| `RUNAS ERROR: 1326` | 비밀번호·계정 형식 또는 로그온 조건 불일치 | 프로세스 미생성 | 로컬·도메인 계정 표기, 비밀번호와 계정 상태 확인 |
| `RUNAS ERROR: 1385` | 대상 계정에 요청한 로그온 유형 권한이 없음 | 프로세스 미생성 | 로컬 로그온 정책을 확인하고 원격 서비스 인증이 목적이면 `/netonly` 기법 검토 |
| 새 프로세스는 열리지만 상승되지 않음 | 계정 전환은 성공했지만 관리자 token은 없음 | 다른 일반 사용자 프로세스 | 사용자 프로필·파일·서비스 접근 범위 재평가 |

## 확인할 출력과 권한

- `runas /user`는 입력한 계정의 로컬 프로세스를 만들며 `/netonly`와 결과가 다르다.
- `Attempting to start`가 아니라 새 프로세스의 `whoami /all`로 계정 전환을 확정한다.
- 관리자 그룹 멤버십만으로 상승된 token을 확정하지 않는다.

## 관련 공격기법

- [[저장된 자격 증명으로 runas 프로세스 실행]]
- [[확보한 AD 비밀번호로 runas netonly 네트워크 인증 컨텍스트 생성]]

## 관련 도구

- [[runas]]

## 관련 상태 라우터

- [[확보한 자격 증명으로 원격 접근 경로 선택]]
- [[Windows 셸 또는 세션 확보 후 컨텍스트 열거]]
- [[고권한 세션 확보 후 후속 판단]]
