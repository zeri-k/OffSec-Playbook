---
tags:
  - 환경/ad
  - 기능/열거
실행환경: ["Windows CMD", "Windows PowerShell"]
필요권한: ["현재 AD 계정에 허용된 GPO와 정책 설정 읽기 권한"]
필요조건: ["도메인 가입 Windows 호스트의 사용자 세션 또는 도메인 사용자 컨텍스트"]
결과: ["GPO별 정책 설정", "GPO 보안 문제 후보", "stdout 또는 log 결과"]
---

# Group3r

## 도구 개요

Group3r는 AD에 연결된 Group Policy Object(GPO)를 읽고 정책 설정별 보안 문제 후보와 판단 근거를 계층화하여 출력하는 도구다. 여러 GPO의 설정과 적용 위험을 한 번에 검토하고 후속 수동 확인이 필요한 정책을 찾을 때 적합하다.

## 필요한 입력과 실행 환경

- 실행 위치: 도메인에 가입된 Windows 호스트 또는 도메인 사용자 컨텍스트의 CMD·PowerShell
- 계정 조건: 일반 도메인 사용자로 시작할 수 있으며 관리자 권한은 필수가 아님
- 출력 입력: stdout을 사용할지 결과 log 파일을 만들지 선택
- 대체 실행 컨텍스트: 현재 로컬 세션과 별도의 도메인 계정이 필요하면 `runas /netonly`로 도구를 실행할 수 있는 프로세스를 준비

## 표준 사용법

```cmd
group3r.exe -s
group3r.exe -f <OUTPUT_LOG>
```

## 대표 예시

### 결과를 파일로 저장

```cmd
group3r.exe -f group3r-findings.log
```

확인할 출력:

- 지정한 log 파일과 GPO, 정책 설정, 발견 사항이 구분된 계층 구조.
- finding은 설정 문제 후보이며 현재 계정의 GPO 쓰기 권한을 뜻하지 않는다.

### 도움말과 stdout 사용

```cmd
group3r.exe -h
group3r.exe -s
```

확인할 출력:

- 현재 버전에서 지원하는 옵션과 콘솔에 표시된 정책별 발견 사항.

## 주요 옵션

| 옵션 | 의미 | 사용하는 상황 |
|---|---|---|
| `-s` | 결과를 stdout으로 출력 | 터미널에서 바로 검토할 때 |
| `-f <FILE>` | 결과를 파일에 저장 | 보고서 근거와 후속 분석 자료를 남길 때 |
| `-h` | 도움말 표시 | 현재 버전 문법 확인 |

## 도구 고유 출력

| 출력·상태 | 의미 | 다음 확인 |
|---|---|---|
| 들여쓰기 없는 항목 | GPO 단위 | 표시 이름, GUID와 링크 대상 OU 확인 |
| 한 단계 들여쓴 항목 | 해당 GPO의 정책 설정 | 적용 범위와 상속 확인 |
| 더 깊게 들여쓴 finding | 설정에서 발견한 문제 후보와 이유 | 실제 레지스트리·정책 값과 영향 호스트 확인 |
| 결과 없음 또는 실행 실패 | 컨텍스트·도달성·읽기 범위 또는 옵션 문제 | 도메인 사용자 컨텍스트, DC 접근과 `-s`·`-f` 지정 확인 |

## 관련 공격기법

- [[AD 보안 구성과 GPO 감사]]
- [[AD GPO 쓰기 권한과 영향 범위 열거]]
