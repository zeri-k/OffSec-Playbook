---
tags:
  - 환경/linux
  - 기능/열거
  - 기능/권한상승
시작상태: ["Linux 셸 확보", "Linux 사용자 세션 확보"]
목표: ["현재 사용자와 권한 확인", "권한 상승 경로 확인", "재사용 자격 증명 또는 내부망 단서 확보"]
현재계정: ["Linux 로컬 사용자", "AD 또는 Kerberos 연동 계정", "미확인"]
현재 가능한 행위: ["셸에서 명령 실행", "SSH 세션에서 명령 실행", "Web Shell에서 명령 실행", "에이전트 세션에서 명령 실행"]
필요권한: ["현재 Linux 사용자 권한"]
필요정보: ["접근한 호스트", "현재 셸 또는 세션"]
네트워크위치: ["접근한 Linux 호스트"]
---

# Linux 셸 확보 후 초기 열거와 권한 상승

## 상태 라우터 개요

Linux 셸을 확보하면 셸 품질, 로컬 권한, 계정 연동, 자격 증명과 네트워크 위치를 독립적으로 확인하고 가장 구체적인 후속 기법으로 전환한다.

## 적용 조건

| 상태 축 | 조건 |
|---|---|
| 대상 플랫폼 | Linux |
| 현재 계정 | 로컬 사용자, 도메인 계정 또는 미확인 상태 |
| 현재 가능한 행위 | 셸·SSH·Web Shell·에이전트 세션에서 명령 실행 |
| 현재 권한 | 일반 사용자부터 root까지 별도 확정 필요 |
| 보유 정보 | 현재 호스트와 세션 |
| 네트워크 위치 | 접근한 Linux 호스트 내부 |
| 목표 | 권한·현재 계정·보유 정보·내부망 위치를 확정해 다음 행동 선택 |

## 판단 경로

| 현재 보유 상태·입력 | 선택할 공격기법 또는 수동 확인 | 성공하면 얻는 상태 | 다음 상태 라우터 | 선택 기준·미충족 시 확인 |
|---|---|---|---|---|
| 대상 Linux 호스트의 셸에서 입력·작업 제어가 불안정함 | [[TTY 업그레이드]] | 안정적인 대화형 셸 | 이 상태 라우터 재개 | 현재 셸의 stdin/stdout, 대상 호스트의 사용 가능한 interpreter·터미널, 네트워크 연결 지속성을 확인 |
| 대상 Linux 호스트의 현재 사용자·sudo·SUID·cron·capability가 미확인 | [[Linux 권한 상승 열거]] | 검증할 권한 상승 후보 | 이 상태 라우터에서 후보별 기법 선택 | `id`의 유효 UID·그룹, sudo 정책 읽기 가능 여부와 대상 파일·작업의 실제 권한을 확인 |
| 현재 Linux 사용자의 `sudo -l` 결과에서 실행 가능한 명령과 조건을 확인함 | [[sudo 권한 오남용]] | EUID 0의 root 셸·root 명령 실행, 다른 사용자 셸 또는 제한된 파일·명령 접근 중 검증된 결과 | root 실행은 [[고권한 세션 확보 후 후속 판단]], 다른 사용자 셸은 이 라우터, 자격 증명 자료는 [[확보한 자격 증명으로 원격 접근 경로 선택]] | 명령 경로·인수 제한·환경 변수·비밀번호 요구, 실제 실행 사용자와 셸 지속 여부·접근 가능한 파일 범위를 확인 |
| 현재 Linux 사용자가 실행할 수 있는 SUID binary와 정확한 경로를 확인함 | [[SUID GTFOBins 권한 상승]] | SUID 소유자의 셸·명령 실행 또는 제한된 파일 읽기 중 검증된 결과 | 소유자가 root이고 명령 실행이 확인되면 [[고권한 세션 확보 후 후속 판단]], 다른 사용자 셸·제한 파일 접근은 이 라우터, 자격 증명 자료는 [[확보한 자격 증명으로 원격 접근 경로 선택]] | binary 소유자·mode·version, GTFOBins 전제와 mount 옵션, 결과가 셸인지 단일 명령·파일 읽기인지 확인 |
| 현재 Linux 사용자가 수정할 수 있는 파일·디렉터리를 다른 사용자의 cron 작업이 실행함 | [[Cron 권한 상승]] | cron 실행 사용자의 셸·명령 실행 또는 검증 후보 유지 | root 실행은 [[고권한 세션 확보 후 후속 판단]], 다른 사용자 실행은 이 라우터, 실행 미확인은 후보 상태로 이 라우터 | cron 항목·실행 주기·사용자, 쓰기 권한, 실제 실행 결과와 원래 파일 복구 절차를 확인 |
| 정확한 kernel·배포판 버전과 적용 가능한 로컬 exploit 후보를 확인함 | [[Kernel Exploit 후보 검증]] | 적용 가능성 판정 또는 상승된 실행 컨텍스트 | 성공 시 [[고권한 세션 확보 후 후속 판단]], 제외 시 이 상태 라우터 | architecture·kernel build·완화책, exploit 전제와 시스템 영향·복구 가능성을 확인 |
| 현재 Linux 사용자가 home·애플리케이션 설정 파일을 읽을 수 있지만 파일 기반 자격 증명 자료를 아직 열거하지 않음 | [[Linux 파일 자격증명 검색]] | 비밀번호·token·접속 대상 후보 | [[확보한 자격 증명으로 원격 접근 경로 선택]] | 파일 읽기 권한, 검색 범위, 값의 최신성과 대상 계정·서비스를 확인 |
| 현재 Linux 사용자가 shell history 파일을 읽을 수 있지만 명령·인수·환경 변수의 자격 증명을 아직 열거하지 않음 | [[Linux Shell History 자격증명 검색]] | 명령에 남은 비밀번호·token·접속 대상 후보 | [[확보한 자격 증명으로 원격 접근 경로 선택]] | history 파일 위치·소유자·최신성과 값의 대상 서비스를 확인 |
| 현재 Linux 사용자가 SSH·TLS 개인키 경로를 읽을 수 있지만 계정별 개인키를 아직 열거하지 않음 | [[Linux 개인키 검색]] | 계정명이 연결된 개인키 후보 | [[확보한 자격 증명으로 원격 접근 경로 선택]] | key 소유자·권한·암호화 여부와 대상 서비스 도달성을 확인 |
| 대상 Linux 호스트에 파일을 쓸 수 있고 대상에서 공격 호스트 HTTP 포트로 연결 가능함 | [[Linux HTTP 파일 반입]] | 대상에 반입한 파일과 무결성 확인 결과 | 이 상태 라우터에서 파일 사용 조건 재평가 | 전용 제공 디렉터리, 대상 writable 경로, `curl`·`wget`·Python 3 중 하나와 양쪽 hash 확인 |
| 대상 Linux 호스트에 파일을 쓸 수 있지만 표준 HTTP·SMB·SSH client를 사용할 수 없음 | [[제한 환경 파일 반입]] | 내장 도구로 복원한 파일과 무결성 확인 결과 | 이 상태 라우터에서 파일 사용 조건 재평가 | Base64 전달 채널, Bash `/dev/tcp`, 양쪽 Nmap Ncat 또는 설치된 대체 binary와 실제 통신 방향 확인 |
| 유효한 SSH 계정으로 대상에 인증할 수 있고 source 읽기·destination 쓰기 권한을 확인함 | [[SCP 파일 반입과 회수]] | SSH 연결로 반입하거나 회수한 파일과 무결성 확인 결과 | 이 상태 라우터에서 파일 사용·분석 조건 재평가 | 전송 방향, local·remote exact 경로, SFTP 지원과 양쪽 hash 확인 |
| 대상 Linux 호스트에서 파일을 읽을 수 있고 공격 호스트의 HTTP·HTTPS 수신 포트에 연결 가능함 | [[Linux HTTP 파일 회수]] | 공격 호스트로 회수한 파일과 무결성 확인 결과 | 이 상태 라우터에서 확보한 파일의 후속 분석 선택 | 대상 파일 ACL, HTTP endpoint, TLS 인증서와 수신 파일 hash 확인 |
| 대상 Linux 호스트에서 읽을 수 있는 keytab 또는 ccache 파일과 그 Kerberos principal을 확인함 | [[Linux Kerberos keytab ccache 악용]] | keytab key로 발급하거나 ccache에서 재사용할 수 있는 ticket | [[AD Identity 확인 후 도메인 컨텍스트 열거]] | 파일 소유자·읽기 권한, principal·realm, ticket 만료와 DC Kerberos 포트 도달성을 확인 |
| 유효 ccache에서 주체·realm·만료가 확인된 service ticket을 확보했지만 사용할 서비스나 목표를 고르지 않음 | 수동 확인: ticket으로 AD 객체를 열거할지 특정 서비스에 인증할지 목표를 구분 | 계정명이 연결된 ticket과 서비스 후보 | AD 객체·권한 목표면 [[AD Identity 확인 후 도메인 컨텍스트 열거]], ticket 사용처 확인이면 [[확보한 자격 증명으로 원격 접근 경로 선택]] | ticket cache 환경 변수, realm·만료, 대상 SPN·FQDN, DNS·시간과 서비스 포트 도달성을 확인 |
| 대상 Linux 호스트에서 추가 인터페이스·route·내부 연결을 확인함 | [[피벗팅 경로 식별과 내부망 열거]] | 새 네트워크 위치와 피벗 후보 | [[내부망 경로 확보 후 피벗 구성]] | 추가 대역 route, 현재 사용자로 가능한 연결·프로세스 실행과 새 대역의 대상 포트 도달성을 확인 |
| 대상 Linux 호스트에서 EUID 0의 root 셸을 확인함 | 수동 확인: root 셸의 호스트·EUID·네트워크 위치를 기록하고 고권한 상태 라우터로 전환 | 확인된 Linux 고권한 세션 | [[고권한 세션 확보 후 후속 판단]] | 실제 EUID가 0인지, 현재 셸이 유지되는지와 추가 네트워크 인터페이스를 확인 |

## 상태 재평가

- 유효 UID, 그룹, sudo 범위와 셸 실행 주체를 구분한다.
- AD join 여부와 Kerberos ticket 보유 여부는 서로 독립적으로 판단한다.
- 자격 증명, 계정, 권한 또는 네트워크 위치가 바뀌면 이 문서의 시작 상태를 다시 적용한다.
- 자동 열거 결과는 후보로만 취급하고 실제 권한·경로·서비스 접근을 해당 기법에서 검증한다.
- 동시에 성립하는 상태를 버리지 않는다. 현재 호스트의 실행 환경·로컬 권한이 목표면 이 라우터를, AD 객체·권한 관계가 목표면 [[AD Identity 확인 후 도메인 컨텍스트 열거]]를, 인증 자료의 사용처가 목표면 [[확보한 자격 증명으로 원격 접근 경로 선택]]을, 다른 대역 도달성이 목표면 [[내부망 경로 확보 후 피벗 구성]]을 선택한다.
- 같은 목표·상태의 명령·기법 실패는 연결된 기법 문서의 실패 분기와 이 라우터에서 처리한다. 목표가 바뀌거나 계정·권한·가능한 행위·네트워크 위치가 바뀌어 다음 라우터가 불명확하면 [[Playbook|홈]]으로 돌아가 상태와 목표를 함께 다시 고른다.
